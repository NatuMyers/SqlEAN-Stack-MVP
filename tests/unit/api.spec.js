var rootController = require('../../public/js/controllers/rootController.js')

describe('root controllers', function() {
  it('should exist', function() {
    expect(rootController).not.to.equal(undefined);
  });
});